name: PR Automated Checks

on:
  pull_request:
    branches: [ master, main ]

jobs:
  security-audit:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: |
          spartan-hub/package-lock.json
          spartan-hub/backend/package-lock.json
    
    - name: Install dependencies
      run: npm ci
      working-directory: ./spartan-hub

    - name: Install backend dependencies
      run: npm ci
      working-directory: ./spartan-hub/backend

    - name: Run security audit
      run: npm audit --audit-level high
      working-directory: ./spartan-hub/backend

  linting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: |
          spartan-hub/package-lock.json
          spartan-hub/backend/package-lock.json
    
    - name: Install dependencies
      run: npm ci
      working-directory: ./spartan-hub

    - name: Install backend dependencies
      run: npm ci
      working-directory: ./spartan-hub/backend

    - name: Run ESLint
      run: npx eslint . --ext .ts,.tsx --max-warnings 0
      working-directory: ./spartan-hub/backend

  tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: |
          spartan-hub/package-lock.json
          spartan-hub/backend/package-lock.json
    
    - name: Install dependencies
      run: npm ci
      working-directory: ./spartan-hub

    - name: Install backend dependencies
      run: npm ci
      working-directory: ./spartan-hub/backend

    - name: Run unit tests
      run: npm test
      working-directory: ./spartan-hub/backend
      env:
        CI: true

  rate-limit-validation:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: |
          spartan-hub/backend/package-lock.json

    - name: Install backend dependencies
      run: npm ci
      working-directory: ./spartan-hub/backend
    
    - name: Verify rate limiting implementation
      run: |
        # Check that rate limiting middleware is properly configured
        if grep -r "express-rate-limit" ./spartan-hub/backend/src/middleware/; then
          echo "✓ Rate limiting middleware found"
        else
          echo "✗ Rate limiting middleware not found"
          exit 1
        fi
        
        # Check for Redis store implementation
        if grep -r "RedisStore" ./spartan-hub/backend/src/middleware/; then
          echo "✓ Redis store for rate limiting found"
        else
          echo "✗ Redis store for rate limiting not found"
          exit 1
        fi
        
        # Check for differentiated rate limits
        if grep -r "authRateLimit\|getRateLimit\|writeRateLimit" ./spartan-hub/backend/src/server.ts; then
          echo "✓ Differentiated rate limits found"
        else
          echo "✗ Differentiated rate limits not found"
          exit 1
        fi

  zod-validation-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: |
          spartan-hub/backend/package-lock.json

    - name: Install backend dependencies
      run: npm ci
      working-directory: ./spartan-hub/backend
    
    - name: Verify Zod validation implementation
      run: |
        # Check that Zod schemas are used for validation
        if grep -r "zod\|Zod" ./spartan-hub/backend/src/middleware/ ./spartan-hub/backend/src/schemas/; then
          echo "✓ Zod validation found"
        else
          echo "✗ Zod validation not found"
          exit 1
        fi

  typescript-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: |
          spartan-hub/backend/package-lock.json

    - name: Install backend dependencies
      run: npm ci
      working-directory: ./spartan-hub/backend

    - name: Run TypeScript type check
      run: npx tsc --noEmit
      working-directory: ./spartan-hub/backend
